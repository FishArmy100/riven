
struct Vec2 {
    x: Float = 0.0,
    y: Float = 0.0,
}

// fn Vec2.add(self, other: Vec2) -> Vec2
// {
//    return Vec2 {
//         x: self.x + other.x,
//         y: self.y + self.y,
//     };
// }

fn main()
{
    let a = Vec2 { x:  5.1 }; // default args
    let b = Vec2 { y: -8.0 };

    // let c = a.add(b); // member function
    // println("[" + c.x + ":" + c.y + "]"); // member access
}